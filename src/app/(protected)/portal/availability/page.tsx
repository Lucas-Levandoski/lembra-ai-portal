import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Minha Disponibilidade'
};

export default function PortalAvailability() {
  return <div> testing dashboard inside portal </div>
}